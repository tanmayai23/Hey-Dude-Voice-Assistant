eel==0.18.1
python-dotenv>=1.0.0
google-generativeai>=0.8.5
pyttsx3>=2.99
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
playsound==1.2.2
pywhatkit>=5.4
pyautogui>=0.9.54
opencv-contrib-python>=4.10.0
# --- Wake word (offline, ~1% CPU) ---
pvporcupine>=3.0.2
pvrecorder>=1.2.2
# --- System control / monitor ---
psutil>=5.9.8
# Windows-only volume + brightness; pip-install on Linux/Mac will fail but
# system_control.py guards every import so non-Windows hosts still boot.
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4.1; sys_platform == "win32"
screen-brightness-control>=0.23.0
# Screenshot capture (cross-platform, fast — no PIL dependency)
mss>=9.0.1
# Used for downloads in Backend/file_ops.py
requests>=2.32.0
